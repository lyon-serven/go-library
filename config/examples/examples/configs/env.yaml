app:
  name: "EnvApp"
  version: "2.0.0"
  environment: "${APP_ENV:-development}"
  debug: false

database:
  host: "${DB_HOST}"
  port: 3306
  username: "${DB_USER:-admin}"
  password: "${DB_PASSWORD}"
  database: "myapp"
  max_open_conns: 10
  max_idle_conns: 5
  timeout: "30s"
